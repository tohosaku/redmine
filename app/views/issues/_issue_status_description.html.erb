<% if issue_statuses.any? {|s| s.description.present? } %>
  <% component 'modal_dialog', id: 'issue_statuses_description', title: l(:label_issue_statuses_description) do %>
    <dl>
      <% issue_statuses.each do |issue_status| %>
      <% if issue_status.description.present? %>
        <dt><%= link_to issue_status.name, '#', data: { action: 'issues--select#select dialog#hide', issues__select_id_param: issue_status.id }, title: l(:text_select_apply_issue_status) %></dt>
        <dd><%= issue_status.description %></dd>
      <% end %>
    <% end %>
    </dl>
  <% end %>
<% end %>
