$('#ajax-modal').append('<template data-modal-dispatcher-target="hide"></template>')
$('#tab-content-users').html('<%= escape_javascript(render :partial => 'groups/users', :locals => {:users_to_highlight => @users.map{|user| user.id}}) %>');
