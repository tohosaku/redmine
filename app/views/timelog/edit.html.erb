<h2><%= l(:label_spent_time) %></h2>

<%= labelled_form_for @time_entry, url: time_entry_path(@time_entry), html: {multipart: true, data: { controller: 'clear form', form_url_value: edit_time_entry_path }} do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= submit_tag l(:button_save) %>
  <%= cancel_button_tag_for_time_entry(@project) %>
<% end %>
